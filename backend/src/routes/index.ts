import { Router } from 'express';

const router = Router();

// Placeholder for future routes
// Will add auth.routes, manager.routes, employee.routes, shift.routes in future phases

export default router;
