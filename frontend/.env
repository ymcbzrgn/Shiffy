# =============================================================================
# Shiffy Frontend - Environment Configuration
# =============================================================================
# ⚠️  This file can be committed (no sensitive data)
# Expo requires EXPO_PUBLIC_ prefix for runtime environment variables

# =============================================================================
# API BASE URL (Backend Server)
# =============================================================================
# For local development with simulator/emulator
# EXPO_PUBLIC_API_BASE_URL=http://localhost:3000

# For testing on physical device, use your computer's local IP:
# Find your IP: ifconfig | grep "inet " | grep -v 127.0.0.1
# Current local IP (updated for WiFi switch):
EXPO_PUBLIC_API_BASE_URL=http://172.20.10.3:3000
# Example: EXPO_PUBLIC_API_BASE_URL=http://192.168.1.100:3000

# =============================================================================
# NOTES
# =============================================================================
# - Restart Expo dev server after changing this file
# - Access in code: process.env.EXPO_PUBLIC_API_BASE_URL
# - Expo only exposes variables with EXPO_PUBLIC_ prefix
